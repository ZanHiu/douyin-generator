# 04 - Local Setup Docs

## Goal

Document local AI setup so the sprint is testable on a fresh machine.

## Tasks

- Document `uv sync` for faster-whisper.
- Document Ollama install/start requirement.
- Document `ollama pull qwen3:4b`.
- Document `.env` provider values.

## Acceptance Criteria

- A developer can switch from OpenAI to local providers by editing `.env`.
