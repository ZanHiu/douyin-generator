# Render Contract

- Extend the edit render request schema with video, audio, and subtitle style fields.
- Add a generic `/api/jobs/{job_id}/edits/render` endpoint.
- Keep the old blur endpoint as an alias during the transition.
