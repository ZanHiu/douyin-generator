# Done Criteria

- `netlify.toml` exists and matches the frontend build
- managed env example exists for Supabase + Upstash + R2 + Northflank
- frontend can resolve API requests and download links through `VITE_API_BASE_URL`
- backend supports configurable cookie/CORS behavior for managed deploys
- one step-by-step doc exists for the full managed stack
