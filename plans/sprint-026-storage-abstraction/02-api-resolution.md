# 02. API Resolution

At read/download/render boundaries:

- resolve job and edit artifact keys back into local paths
- keep download URLs unchanged
- keep support for legacy rows that still contain absolute paths
