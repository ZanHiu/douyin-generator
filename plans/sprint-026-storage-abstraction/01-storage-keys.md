# 01. Storage Keys

Persist storage references as relative keys:

- `jobs/<folder>/...`
- `edits/<job-folder>/<edit-folder>/...`

Do not change the local filesystem layout yet. This sprint only changes how references are stored and resolved.
