# 01. Settings Contract

Per-user settings for current surfaced controls:

- default voice
- burn subtitles on new jobs
- mix original audio on new jobs
- default voice volume
- default original audio volume
- default subtitle font size
- default subtitle position
- default subtitle text color
- default overlay font size
- default overlay text color

These settings are defaults, not hard locks. Generate and Editor can still override them.
