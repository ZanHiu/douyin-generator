# 04. Tests And Done

- Run backend compile checks for new settings modules.
- Run frontend typecheck.
- Verify:
  - login -> settings load
  - save settings
  - refresh page and see saved defaults
  - open Generate and confirm prefilled values
  - create new job and confirm final render baseline uses the snapped defaults
