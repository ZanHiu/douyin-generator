# Done Criteria

- Oracle deploy path is documented end to end
- frontend bind host is configurable in production compose
- there is a concrete reverse proxy example for domain + HTTPS
- README and production docs point to the Oracle playbook
