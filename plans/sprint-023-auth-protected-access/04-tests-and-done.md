# 04. Tests And Done

- Run backend compile checks for new auth modules.
- Run frontend typecheck.
- Verify login -> app access -> logout flow.
- Verify unauthenticated access to `/`, `/jobs`, `/editor`, `/settings`, and `/api/jobs` is blocked.
